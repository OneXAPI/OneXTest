{
    "tasks": [
        {
            "type": "shell",
            "label": "C/C++: g++ 활성 파일 빌드",
            "command": "/usr/bin/make",
            "options": {
                "cwd": "${fileDirname}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "디버거에서 생성된 작업입니다."
        }
    ],
    "version": "2.0.0"
}